# Image Generation brief

Complete this checklist, then encode it in the typed manifest.

Image kind and intended use:
Operation and actual source/template inputs:
Profile ID and concrete overrides:
Aspect ratio, target pixels, background and alpha requirement:
Composition, reading order and relationships:
Exact displayed text by region (no prose rewrite):
Negative constraints and frozen features:
Output IDs, consistency anchor and batch size:
Visual/text QA and separate export step:
