# Evolve the protocol and its published reference

Classify the change as schema, route, profile, capability fact or operator preference. Edit upstream, update tests, regenerate matching exports, compare hashes, and explicitly migrate active packs.

Load `../docs/extensibility.md` for the procedure and constraints; load `../registry/capabilities.json`
only when selecting or re-verifying a UI route. All paths are relative to this skill.

Verify: the relevant documented acceptance checks pass and unresolved gates are named.
Do not copy the full reference manual into this route or the skill body.
