# Prepare image-generation and editing turns

Resolve the selected profile and actual template/source assets. Separate exact displayed text from composition. Use small batches and an independent QA/export step. Check that an image turn asks for images only.

Load `../docs/images.md` for the procedure and constraints; load `../registry/capabilities.json`
only when selecting or re-verifying a UI route. All paths are relative to this skill.

Verify: the relevant documented acceptance checks pass and unresolved gates are named.
Do not copy the full reference manual into this route or the skill body.
