# Resume or ingest returned artifacts

Read the expected output IDs and original manifest digest. Capture actual files, not only chat links. Validate a handback and import into a new quarantine directory. Review before proposing upstream changes.

Load `../docs/handback.md` for the procedure and constraints; load `../registry/capabilities.json`
only when selecting or re-verifying a UI route. All paths are relative to this skill.

Verify: the relevant documented acceptance checks pass and unresolved gates are named.
Do not copy the full reference manual into this route or the skill body.
