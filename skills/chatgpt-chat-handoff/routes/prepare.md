# Prepare a bounded consultation

Read the field dictionary and account routing. Choose the smallest sufficient capability, gather only approved inputs, scaffold a draft, then fill every decision and acceptance condition. Validate before rendering or bundling.

Load `../docs/protocol.md` for the procedure and constraints; load `../registry/capabilities.json`
only when selecting or re-verifying a UI route. All paths are relative to this skill.

Verify: the relevant documented acceptance checks pass and unresolved gates are named.
Do not copy the full reference manual into this route or the skill body.
