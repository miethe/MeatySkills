# Adopt without duplicating an existing handoff system

Read the duplicate gate and preserve existing AOS packet ownership. Preview the instruction patches, then run the official local gates. Verify adoption with actual command receipts, not directory presence.

Load `../docs/bootstrap.md` for the procedure and constraints; load `../registry/capabilities.json`
only when selecting or re-verifying a UI route. All paths are relative to this skill.

Verify: the relevant documented acceptance checks pass and unresolved gates are named.
Do not copy the full reference manual into this route or the skill body.
