# Prepare reasoning, code, data, visual and artifact tasks

Bind environment and input evidence. State which outputs need runnable files or native editability. Keep code execution local unless a safe sandbox test is explicitly in scope. Use the relevant template.

Load `../docs/analysis-artifacts.md` for the procedure and constraints; load `../registry/capabilities.json`
only when selecting or re-verifying a UI route. All paths are relative to this skill.

Verify: the relevant documented acceptance checks pass and unresolved gates are named.
Do not copy the full reference manual into this route or the skill body.
